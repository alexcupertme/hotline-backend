export interface IUser {
    id: string
    email: string
    nickname: string
    password: string
    firstName: string
    lastName: string
    sessionId: string
    createdAt: Date
    updatedAt: Date
}
